# 跨平台行尾规范（macOS / Linux / Windows 统一）。
#
# 让 git 自动识别文本文件并统一用 LF 行尾——仓库存 LF、工作区也 LF，
# 任何系统都一样。该规则跟随仓库生效，contributor 无需各自配置 core.autocrlf。
* text=auto eol=lf

# 前端构建产物（vite 生成的 minified 文件，diff 无意义）：按二进制处理，
# git 不做行尾转换，避免 CRLF/LF 噪声。
internal/web/assets/assets/* binary
