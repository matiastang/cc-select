# >>> cc-select shell integration (managed by cc-select; do not edit between markers) >>>
# {{.Comment}}
ccs() {
  if [ "$#" -gt 0 ] && [ "$1" = "use" ]; then
    eval "$("{{.BinaryPath}}" use "${@:2}")"
  else
    "{{.BinaryPath}}" "$@"
  fi
}
# <<< cc-select shell integration <<<
